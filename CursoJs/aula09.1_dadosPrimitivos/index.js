// String, Number, undefined, null, boolean
const nome = "Tomás"; //String
const nome1 = `Túlio`; //String
const num1 = 10; //Number
const num2 = 10.52; //Number
let nomeAluno; //Undefined = indefinido = Não aponta pra local nenhum na memória
const sobreNome = null; //Nulo -> Não aponta pra local nenhum na memória
const aprovado = true; //Boolean = pode ser True ou False (Lógico)

console.log(typeof nome, nome); //Ver o tipo
console.log(typeof num1, num1); //Ver o tipo
console.log(typeof num2, num2); //Ver o tipo
console.log(typeof nomeAluno, nomeAluno); //Ver o tipo
console.log(typeof sobreNome, sobreNome); //Ver o tipo
console.log(typeof aprovado, aprovado); //Ver o tipo
